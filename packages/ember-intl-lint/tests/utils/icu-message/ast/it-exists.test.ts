import { assert, test } from '@codemod-utils/tests';

import { AST } from '../../../../src/utils/icu-message/index.js';

test('utils | icu-message | ast > it exists', function () {
  assert.ok(AST);
});
