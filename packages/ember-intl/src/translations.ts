import type { Translations } from './-private/utils/translations.ts';

/**
 * NOTE: this is a generated file that is produced at build time during
 * `treeForAddon`, if `publicOnly` is disabled.
 *
 * @private
 * @hide
 */
const translations: readonly [string, Translations][] = [];

export default translations;
